# Q2. write a function calculate_area(length, with=10) that returns the area of a rectangle. test it by calling the d=function with.
# 1. both length and width
# 2. only length (use defulata width)

def calculate_area(length, width=10):
    return print(length*width)

d = calculate_area(5, 5)
b = calculate_area(5)

