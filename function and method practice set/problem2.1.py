# function arguments & return Value
# Q1 write a function full_name(first, last) that takes first name and last name as a parameter and returns a single string in the format

def full_name(first, last):
    return print(f"{first} {last}")
full_name("abhik", "soni")